{
  "name": "realtime-chat-app",
  "version": "1.0.0",
  "description": "リアルタイムチャットアプリケーション",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node --import=./geocoder-compat.mjs --import=./points-persistence.mjs --import=./theme-persistence.mjs --import=./media-delete.mjs --import=./firebase-persistence.mjs server.js",
    "build": "echo \"No build step required for this Node.js server\"",
    "dev": "node --import=./geocoder-compat.mjs --import=./points-persistence.mjs --import=./theme-persistence.mjs --import=./media-delete.mjs --import=./firebase-persistence.mjs server.js"
  },
  "keywords": [
    "chat",
    "map",
    "gemini"
  ],
  "author": "",
  "license": "ISC",
  "license": "ISC",
  "dependencies": {
    "@google/genai": "^1.52.0",
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^4.18.2",
    "socket.io": "^4.7.2"
  }
}
